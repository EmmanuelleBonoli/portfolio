import { NavLink } from "react-router-dom";

const Footer = () => {
    return (
        <div className="footer">
            <NavLink to="#home">Retour en haut</NavLink>
        </div>
    );
};

export default Footer;